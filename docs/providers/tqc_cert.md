---
provider_id: tqc_cert
status: partial
site: default
last_verified: 2026-08-01
---

# `tqc_cert` — TQC certification sample-paper archive

<!-- BEGIN GENERATED: source-inventory -->
> Generated by `uv run python scripts/render_docs.py`; do not edit this block.

| Field | Inventory value |
| --- | --- |
| Status | partial |
| Status reason | Partial / exact 44-row listing retained but nine payloads are wrong after storage-key collisions and redistribution remains unresolved |
| Category | TQC certification sample-paper archive |
| Available years | 2015–2026 |
| Availability note | 2015–2026 observed |
| Local year buckets | 11 |
| Raw event pages | 11 |
| Normalized paper records | 44 |
| Sync failures | 0 |
| Discovery snapshot | present / partial; [`data/providers/tqc_cert/source-manifest.json`](../../data/providers/tqc_cert/source-manifest.json) |
| Publication | quarantined: wrong_payload — Nine public records point at the wrong bytes after five generic-label storage-key collisions overwrote distinct source PDFs. |

Official sources:
- [https://www.tqc.org.tw/TQCNet/ExamPaper.aspx](https://www.tqc.org.tw/TQCNet/ExamPaper.aspx)

Restrictions:
- The four-page source is a sample-paper listing rather than a complete sitting archive; TQC+ remains a separate unapproved family.
- Nine of 44 public records point to wrong bytes because five generic-label groups collide on storage keys.
- No sample-paper republication grant was established; broader CSF pages prohibit reposting/excerpting without authorization.

Inventory evidence:
- [`docs/providers/tqc_cert.md`](tqc_cert.md)
<!-- END GENERATED: source-inventory -->

## Source boundary

Use official TQC examination artifacts with source identity strong enough to keep distinct payloads separate. Generic labels must not share storage keys when their source files differ.

## Gaps and blockers

Treat the generated status, restrictions, years, and counts as current state. Keep durable explanations of source behavior here, and update `catalog/source-inventory.json` instead of restating changing measurements in prose.

## Publication shape

The provider owns `data/providers/tqc_cert/` and `mirror/providers/tqc_cert/`. Site membership, bundle selection, and release-shard ownership belong to `app/site_registry.py` and the publication contracts, not to the provider adapter.

## Operating it

```bash
uv run python -m app discover --provider tqc_cert
uv run python -m app sync-incremental --provider tqc_cert --site-id default
uv run python -m app publish-site --site-id default --repository <owner>/<repo>
```

Run the strict catalog and history audits from [catalog audit](../operations/catalog-audit.md) before publication.

## Open decisions

No provider-specific open decision is recorded here. Put durable architectural choices in `docs/decisions/`; use an issue or temporary untracked plan for unfinished execution work.
