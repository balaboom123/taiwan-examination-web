---
provider_id: taigi_cert
status: partial
site: default
last_verified: 2026-08-01
---

# `taigi_cert` — Ministry of Education Taiwanese-language certification sample papers/audio

<!-- BEGIN GENERATED: source-inventory -->
> Generated by `uv run python scripts/render_docs.py`; do not edit this block.

| Field | Inventory value |
| --- | --- |
| Status | partial |
| Status reason | Partial / exact current A/B/C URLs retained, but source dates are unknown, public taxonomy is merged, and robots/licensing block refresh and release; local coverage: synthetic 2026; 3 → 35 |
| Category | Ministry of Education Taiwanese-language certification sample papers/audio |
| Available years | unknown |
| Availability note | The official A/B/C sample-resource groups are undated; AD 2026 is only the adapter capture year |
| Local year buckets | 1 |
| Raw event pages | 3 |
| Normalized paper records | 35 |
| Sync failures | 0 |
| Discovery snapshot | present / partial; [`data/providers/taigi_cert/source-manifest.json`](../../data/providers/taigi_cert/source-manifest.json) |
| Publication | quarantined: wrong_identity — All 35 records carry a synthetic 2026 identity for undated source material, and one public bundle merges the official A, B, and C paper forms into a single form. |

Official sources:
- [https://ttg.moe.edu.tw/tmt/view.php?page=resource](https://ttg.moe.edu.tw/tmt/view.php?page=resource)

Restrictions:
- All 35 retained A/B/C URLs are current, but the source is undated and all records use an unsupported synthetic 2026 year.
- The public A-form bundle merges A, B, and C records, obscuring source taxonomy.
- Official robots rules disallow generic crawling and the /tmt/src/ asset path; no explicit redistribution grant was found.

Inventory evidence:
- [`docs/providers/taigi_cert.md`](taigi_cert.md)
<!-- END GENERATED: source-inventory -->

## Source boundary

Use official Taiwanese-language certification paper forms while preserving form variants and the absence of stable event years. Fetch time must not become examination time.

## Gaps and blockers

Treat the generated status, restrictions, years, and counts as current state. Keep durable explanations of source behavior here, and update `catalog/source-inventory.json` instead of restating changing measurements in prose.

## Publication shape

The provider owns `data/providers/taigi_cert/` and `mirror/providers/taigi_cert/`. Site membership, bundle selection, and release-shard ownership belong to `app/site_registry.py` and the publication contracts, not to the provider adapter.

## Operating it

```bash
uv run python -m app discover --provider taigi_cert
uv run python -m app sync-incremental --provider taigi_cert --site-id default
uv run python -m app publish-site --site-id default --repository <owner>/<repo>
```

Run the strict catalog and history audits from [catalog audit](../operations/catalog-audit.md) before publication.

## Open decisions

No provider-specific open decision is recorded here. Put durable architectural choices in `docs/decisions/`; use an issue or temporary untracked plan for unfinished execution work.
