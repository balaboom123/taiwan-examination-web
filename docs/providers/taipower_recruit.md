---
provider_id: taipower_recruit
status: partial
site: default
last_verified: 2026-08-01
---

# `taipower_recruit` — Taipower recruitment written papers and answer keys

<!-- BEGIN GENERATED: source-inventory -->
> Generated by `uv run python scripts/render_docs.py`; do not edit this block.

| Field | Inventory value |
| --- | --- |
| Status | partial |
| Status reason | Partial: the official event set is represented by a 23-event manifest, but retained state has 21 events/370 records, omits both ROC 107 sessions, and is demonstrably first-page-truncated for modern years; live full-asset enumeration is currently WAF-blocked. |
| Category | Taipower recruitment written papers and answer keys |
| Available years | 2001–2026 |
| Availability note | 23 official event tabs across 22 years: ROC 90, 92, 94–97, 99, 101–106, 107 May and December, and 108–115 / AD 2001–2026 with gaps |
| Local year buckets | 21 |
| Raw event pages | 21 |
| Normalized paper records | 370 |
| Sync failures | 0 |
| Discovery snapshot | present / partial; [`data/providers/taipower_recruit/source-manifest.json`](../../data/providers/taipower_recruit/source-manifest.json) |
| Publication | eligible for site publication policy |

Official sources:
- [https://www.taipower.com.tw/2289/2544/2554/2557/](https://www.taipower.com.tw/2289/2544/2554/2557/)

Restrictions:
- The latest indexed official HTML exposes 23 event tabs, but current direct listing and asset requests receive a zero-byte AWS WAF challenge; the hc1/hc2 delivery hosts time out from this environment.
- The old client fetched only the default first 10 subject groups per event and did not recognize the ROC 107 May/December tabs; official filtered listings prove at least four modern events are truncated and both 2018 sessions are absent locally.
- All 370 retained records have unique official Taipower media URLs discovered under the /2557 archive and unique checksums, but the incorrect MOEA sibling state duplicates them exactly; eight additional unreferenced mirror PDFs (3,570,035 bytes) are MOEA new-staff material and remain undeleted pending migration approval.
- Taipower's open-data declaration permits attributed reuse within Taipower-owned rights with exclusions; it does not independently prove ownership of every exam item.

Inventory evidence:
- [`docs/providers/taipower_recruit.md`](taipower_recruit.md)
<!-- END GENERATED: source-inventory -->

## Source boundary

Use official Taipower recruitment examination events and administered paper attachments. Corporate recruitment information that is not an administered paper remains outside the boundary.

## Gaps and blockers

Treat the generated status, restrictions, years, and counts as current state. Keep durable explanations of source behavior here, and update `catalog/source-inventory.json` instead of restating changing measurements in prose.

## Publication shape

The provider owns `data/providers/taipower_recruit/` and `mirror/providers/taipower_recruit/`. Site membership, bundle selection, and release-shard ownership belong to `app/site_registry.py` and the publication contracts, not to the provider adapter.

## Operating it

```bash
uv run python -m app discover --provider taipower_recruit
uv run python -m app sync-incremental --provider taipower_recruit --site-id default
uv run python -m app publish-site --site-id default --repository <owner>/<repo>
```

Run the strict catalog and history audits from [catalog audit](../operations/catalog-audit.md) before publication.

## Open decisions

No provider-specific open decision is recorded here. Put durable architectural choices in `docs/decisions/`; use an issue or temporary untracked plan for unfinished execution work.
