name: discover

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Run discovery
        run: python -m app discover > discover.json

      - name: Upload discovery artifact
        uses: actions/upload-artifact@v7
        with:
          name: moex-discover
          path: discover.json

