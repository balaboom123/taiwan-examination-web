name: sync-tqc-cert

on:
  workflow_dispatch:
  schedule:
    - cron: "15 6 * * 2"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: sync-tqc-cert
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Cache mirror directory
        uses: actions/cache@v6
        with:
          path: mirror
          key: tqc-cert-mirror-${{ hashFiles('data/providers/tqc_cert/source-manifest.json') }}
          restore-keys: |
            tqc-cert-mirror-

      - name: Run TQC cert sync
        run: python -m app sync-full --provider tqc_cert --site-id default

      - name: Commit regenerated TQC cert provider data
        # sync-full writes the papers it did fetch and records the ones it could
        # not, then exits non-zero. Gating this step on success discarded the
        # whole refresh whenever one upstream file was unavailable, and kept the
        # failure record out of the repository where it could be acted on.
        if: '!cancelled()'
        run: >-
          bash .github/scripts/commit-and-push.sh "chore: refresh TQC cert provider data" data/providers/tqc_cert
